# TODO delete once test is validated
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: test-vault-secret
  namespace: external-secrets
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: test-vault-secret
  data:
    - secretKey: foo
      remoteRef:
        key: test/demo
        property: foo
