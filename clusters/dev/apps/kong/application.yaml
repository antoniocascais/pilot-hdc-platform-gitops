apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kong
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "9"
    # NO finalizer — has PostgreSQL StatefulSet
spec:
  project: default
  source:
    repoURL: https://github.com/PilotDataPlatform/pilot-hdc-platform-gitops.git
    targetRevision: main
    path: clusters/dev/apps/kong
    helm:
      valueFiles:
        - ../../registry.yaml
        - ../../versions.yaml
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: utility
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
