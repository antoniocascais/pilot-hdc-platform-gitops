apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: message-bus-greenroom-credentials
  namespace: greenroom
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: message-bus-greenroom-credentials
  data:
    - secretKey: rabbitmq-password
      remoteRef:
        key: secret/data/rabbitmq
        property: password
